# React Datepicker

This is a Datepicker library that operates in React.

## Installation

```
npm install @shinyongjun/react-datepicker
yarn add @shinyongjun/react-datepicker
```

## Usage Datepicker

```tsx
import { Datepicker } from '@shinyongjun/react-datepicker';

function ExampleComponent() {
  return <Datepicker />;
}

export default ExampleComponent;
```

## Usage Rangepicker

```tsx
import { Rangepicker } from '@shinyongjun/react-datepicker';

function ExampleComponent() {
  return <Rangepicker />;
}

export default ExampleComponent;
```

## Props

<PropsTable
  propsData={{
    name: 'initValue',
    component: '<Datepicker />',
    required: 'false',
    type: 'Date | null',
    default: 'null',
    description:
      'Datepicker component allows you to declare the initial value.',
  }}
/>

<PropsTable
  propsData={{
    name: 'onChange',
    component: '<Datepicker />',
    required: 'false',
    type: '(value: Date | null) => void',
    description: 'Triggered whenever the value of the Datepicker changes.',
  }}
/>

<PropsTable
  propsData={{
    name: 'useClearButton',
    component: '<Datepicker />, <Rangepicker />',
    required: 'false',
    type: 'boolean',
    default: 'false',
    description: 'Whether to use the Clear button.',
  }}
/>

<PropsTable
  propsData={{
    name: 'valueFormat',
    component: '<Datepicker />, <Rangepicker />',
    required: 'false',
    type: 'string',
    default: 'YYYY-MM-DD',
    description: 'It must contain "YYYY", "MM", and "DD" in the value.',
  }}
/>

<PropsTable
  propsData={{
    name: 'labelFormat',
    component: '<Datepicker />, <Rangepicker />',
    required: 'false',
    type: 'string',
    default: 'YYYY / MM',
    description: 'It must contain "YYYY" and "MM" in the value.',
  }}
/>

<PropsTable
  propsData={{
    name: 'showsMultipleCalendar',
    component: '<Datepicker />, <Rangepicker />',
    required: 'false',
    type: 'boolean',
    default: 'false',
    description: 'If "true", you can view two calendars.',
  }}
/>
